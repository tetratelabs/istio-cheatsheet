'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
var MS_PATTERN = /^ms-/;
var UPPERCASE_PATTERN = /([A-Z])/g;

var hyphenate = function hyphenate(str) {
  return str.replace(UPPERCASE_PATTERN, '-$1').toLowerCase();
};

var hyphenateStyleName = function hyphenateStyleName(str) {
  return hyphenate(str).replace(MS_PATTERN, '-ms-');
};

exports.default = hyphenateStyleName;